export default class LayerToggle implements ITool{
    Id: string;
    Type: string;

    constructor(id:string,type:string){
        this.Id = id
        this.Type = type
    }

    LayerToggleOnClick() {
    }
}